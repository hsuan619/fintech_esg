import streamlit as st

from ui import tab_pdf_to_json, tab_risk_assessment


# 設定頁面配置 (必須是第一個 Streamlit 指令)
st.set_page_config(page_title="ESG 目標探勘者", layout="wide", page_icon="🌱")

# ==========================================
# Streamlit 網頁介面 (Web Interface)
# ==========================================

st.title("🌱 ESG-Goal-Miner｜ESG 目標探勘者")
st.markdown(
    """
本工具提供從 **PDF 報告→目標 JSON** 到 **績效風險稽核** 的一站式流程。  
步驟 1：上傳 PDF 自動擷取承諾目標；步驟 2：上傳 JSON 進行績效追蹤與風險評估。
"""
)

# 建立兩個主要頁籤
tab1, tab2 = st.tabs(
    [
        "📄 1. 上傳報告並產出目標 JSON (PDF → JSON)",
        "📊 2. 績效追蹤與風險評估",
    ]
)

# ------------------------------------------
# Tab 1: 報告上傳與目標擷取 (PDF -> JSON)
# ------------------------------------------
with tab1:
    tab_pdf_to_json.render()

# ------------------------------------------
# Tab 2: 績效追蹤與風險評估 (核心邏輯區)
# ------------------------------------------
with tab2:
    tab_risk_assessment.render()
